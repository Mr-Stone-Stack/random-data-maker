//It will take an array, or object of items
//then return an array with pointers to the items location in memroy, but in the required order.
//NOTE: This functionality might be the result of the page being compiled when its loaded in the browser
function sortBy(option, array){
    switch(option){
        case 'category':
            
        break;
    }
}

export default {
    sortBy,
}